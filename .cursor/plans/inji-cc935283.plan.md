<!-- cc935283-fb12-4af8-a141-5d55bcfc8cfd 95a17e7e-82fa-47a4-942a-1fd1b57d41bd -->
# INJI Web MVP Integration Plan

## 1. Prepare MOSIP / INJI configuration

- Document required env vars (`ESIGNET_*`, `INJI_CERTIFY_*`, `MIMOTO_URL`) in `.env` and `docs/integration-guide.md`.
- Confirm available eSignet client credentials and INJI Certify API keys; add placeholder values in `.env.example`.

## 2. Backend endpoints for INJI flows

- Update `authController` + `esignetService` to expose `/api/auth/esignet/login` and `/api/auth/esignet/callback` aligned with eSignet expectations.
- Ensure `credentialService` issues VCs via `injiCertifyService.issueCredential` when an `X-ESIGNET-TOKEN` header is present (fallback to mock signing).
- Extend `verificationService` + `verifyController` to call INJI Verify’s API (or `injiCertifyService.verifyCredential`) for signature validation when tokens are available.

## 3. Frontend + INJI Web alignment

- Document how exporters/QAs trigger issuance but direct holders to INJI Web for wallet actions.
- Add instructions to `docs/user-guide.md` for launching the INJI Web app (Docker or hosted) and pointing it at AgriQCert/Mimoto endpoints.
- Provide example configs (`inji-web/env.config.js`) showing `MIMOTO_URL`, issuer IDs, and language defaults.

## 4. Testing & verification

- Manual test checklist covering: eSignet login via AgriQCert, issuing a VC consumed by INJI Web, presenting the VC back to AgriQCert (OpenID4VP or JSON upload).
- Capture known limitations (e.g., fallback signature logic) and next steps for deeper wallet/mobile integration in `docs/integration-guide.md`.

### To-dos

- [ ] Add env vars + integration guide for eSignet/INJI
- [ ] Update auth + VC services for eSignet/INJI APIs
- [ ] Document INJI Web setup + config with AgriQCert
- [ ] Add manual test checklist for end-to-end flow