version: '3.9'

services:
  api:
    build:
      context: ./backend
    ports:
      - '4000:4000'
    environment:
      PORT: 4000
      PUBLIC_URL: http://localhost:4000
      VERIFY_PORTAL_URL: http://localhost:4173/verify
      JWT_ACCESS_SECRET: dev-access-secret
      JWT_REFRESH_SECRET: dev-refresh-secret
      ACCESS_TOKEN_TTL: 15m
      REFRESH_TOKEN_TTL: 7d
      DEFAULT_ADMIN_EMAIL: admin@agriqcert.test
      DEFAULT_ADMIN_PASSWORD: Admin@123
      # Supabase Database Configuration
      # Set these in your .env file or override here
      DATABASE_URL: ${DATABASE_URL:-postgresql://postgres:Qwerty%40uiop%40123@db.gshvnhukuineedxnrtqx.supabase.co:5432/postgres}
      DATABASE_SSL: ${DATABASE_SSL:-true}
    volumes:
      - ./backend/uploads:/app/uploads
      - ./backend/src/data:/app/src/data

  web:
    build:
      context: ./frontend
      args:
        VITE_API_BASE_URL: http://localhost:4000/api
    depends_on:
      - api
    ports:
      - '4173:80'




